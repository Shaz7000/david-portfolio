// Single Apple-style easing curve used everywhere.
// Don't add new curves here without strong reason — consistency > novelty.
export const APPLE_EASE = [0.22, 1, 0.36, 1];

// Common durations (seconds).
export const DURATION = {
  micro: 0.2,
  short: 0.4,
  base: 0.6,
  long: 0.9,
};
